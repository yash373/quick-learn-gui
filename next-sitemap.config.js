/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: process.env.SITE_URL || 'https://quick-learn-gui.vercel.app/', // Change this to your site's URL
    generateRobotsTxt: true, // (Optional) Generate robots.txt file
    // Additional options
};  